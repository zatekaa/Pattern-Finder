# 📝 Git Шпаргалка - Быстрый Справочник

## 🚀 ОСНОВНЫЕ КОМАНДЫ (используйте каждый день)

### Получить последние изменения с GitHub:
```powershell
git pull
```

### Посмотреть что изменилось:
```powershell
git status
```

### Загрузить изменения на GitHub:
```powershell
# Способ 1: Через скрипт (РЕКОМЕНДУЕТСЯ)
.\update.ps1

# Способ 2: Вручную
git add .
git commit -m "Описание изменений"
git push
```

---

## 📋 ЧАСТЫЕ СЦЕНАРИИ

### Начало рабочего дня:
```powershell
cd "c:\Users\HP\Desktop\second version"
git pull
```

### Конец рабочего дня:
```powershell
.\update.ps1
# Введите описание: "Работа за сегодня"
```

### Посмотреть историю изменений:
```powershell
git log --oneline -10
```

### Отменить последний коммит (НЕ загруженный на GitHub):
```powershell
git reset --soft HEAD~1
```

---

## 🔧 НАСТРОЙКА (один раз)

### Установить имя и email:
```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "your@email.com"
```

### Посмотреть настройки:
```powershell
git config --list
```

---

## 🌐 РАБОТА С GITHUB

### Подключить к GitHub (первый раз):
```powershell
git remote add origin https://github.com/USERNAME/murodtrader-app.git
git push -u origin main
```

### Посмотреть подключенные репозитории:
```powershell
git remote -v
```

### Изменить URL репозитория:
```powershell
git remote set-url origin https://github.com/NEW_USERNAME/murodtrader-app.git
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "Your branch is behind"
```powershell
git pull
```

### Ошибка: "failed to push"
```powershell
git pull --rebase
git push
```

### Отменить ВСЕ локальные изменения:
```powershell
git reset --hard
git clean -fd
```

### Посмотреть что изменилось в файле:
```powershell
git diff filename.js
```

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

### Посмотреть изменения перед коммитом:
```powershell
git diff
```

### Добавить только определенный файл:
```powershell
git add filename.js
```

### Изменить последний коммит:
```powershell
git commit --amend -m "Новое описание"
```

### Посмотреть кто изменил файл:
```powershell
git blame filename.js
```

---

## 🎯 ТИПИЧНЫЙ РАБОЧИЙ ПРОЦЕСС

```powershell
# 1. Утро - получить изменения
git pull

# 2. Работа - изменяете файлы
# ...

# 3. Проверка - что изменилось
git status

# 4. Загрузка - отправить на GitHub
.\update.ps1

# 5. Проверка - сайт обновился через 1-2 мин
# https://murod77.netlify.app
```

---

## 💡 СОВЕТЫ

✅ **Делайте коммиты часто** - лучше 10 маленьких, чем 1 большой  
✅ **Пишите понятные описания** - "Исправил баг" лучше чем "fix"  
✅ **Делайте git pull перед работой** - избежите конфликтов  
✅ **Используйте .\update.ps1** - это проще и быстрее  

---

## 🚫 НЕ ДЕЛАЙТЕ

❌ Не коммитьте `.env` файлы (пароли и ключи)  
❌ Не коммитьте `node_modules/` (слишком большие)  
❌ Не делайте `git push --force` (потеряете изменения)  
❌ Не работайте без `git pull` (будут конфликты)  

---

## 📞 БЫСТРАЯ ПОМОЩЬ

**Забыли что делать?**
```powershell
git status  # Покажет текущее состояние
```

**Что-то сломалось?**
```powershell
git log     # Посмотрите историю
git diff    # Посмотрите изменения
```

**Хотите начать заново?**
```powershell
git reset --hard origin/main  # ОСТОРОЖНО! Удалит все локальные изменения
```

---

**Сохраните эту шпаргалку!** 📌
