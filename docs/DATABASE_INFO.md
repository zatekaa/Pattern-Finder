# 📊 База данных активов - Документация

## Обзор

Автоматизированная база данных для анализа паттернов содержит **400+ активов** из разных категорий.

## Структура файлов

### 1. **crypto-database.js** (100+ криптовалют)
- Top криптовалюты: BTC, ETH, BNB, SOL, XRP, ADA, DOGE и др.
- DeFi токены: AAVE, UNI, SUSHI, CRV, YFI и др.
- Мем-коины: PEPE, SHIB, WIF, BONK, FLOKI
- Новые проекты 2024-2025: SEI, SUI, TIA, JUP, STRK
- Layer 2: ARB, OP, METIS, MANTA
- AI токены: FET, AGIX, OCEAN, RNDR

### 2. **stocks-database.js** (150+ акций)
- **Tech Giants**: AAPL, MSFT, GOOGL, AMZN, META, NVDA, TSLA
- **Semiconductors**: AMD, INTC, QCOM, TSM, ASML, AVGO
- **Software & Cloud**: CRM, ADBE, NOW, SNOW, WDAY
- **Cybersecurity**: PANW, CRWD, ZS, FTNT
- **Financial**: JPM, BAC, GS, V, MA, PYPL
- **Healthcare**: JNJ, UNH, PFE, ABBV, LLY
- **Consumer**: WMT, HD, COST, NKE, SBUX, MCD
- **Energy**: XOM, CVX, COP, SLB
- **Automotive & EV**: F, GM, RIVN, LCID, NIO, XPEV
- **E-commerce**: BABA, JD, PDD, SHOP, MELI
- **Gaming**: RBLX, EA, TTWO, ATVI

### 3. **indices-database.js** (35+ индексов)
- **США**: ^GSPC (S&P 500), ^DJI (Dow Jones), ^IXIC (NASDAQ), ^RUT (Russell 2000), ^VIX
- **Европа**: ^FTSE (UK), ^GDAXI (Germany), ^FCHI (France), ^STOXX50E
- **Азия**: ^N225 (Japan), ^HSI (Hong Kong), ^KS11 (Korea), ^BSESN (India)
- **Другие регионы**: ^AXJO (Australia), ^BVSP (Brazil), ^MXX (Mexico)

### 4. **forex-database.js** (50+ пар)
- **Мажорные пары**: EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, NZDUSD
- **Кросс-пары EUR**: EURGBP, EURJPY, EURCHF, EURAUD, EURCAD
- **Кросс-пары GBP**: GBPJPY, GBPCHF, GBPAUD, GBPCAD
- **Кросс-пары JPY**: AUDJPY, CADJPY, CHFJPY, NZDJPY
- **Экзотические пары**: USDTRY, USDZAR, USDMXN, USDBRL, USDRUB, USDINR

### 5. **etf-database.js** (80+ ETF фондов)
- **Широкий рынок**: SPY, VOO, VTI, QQQ, DIA, IWM
- **Технологии**: XLK, VGT, SOXX, SMH, ARKK, ARKW
- **Секторальные**: XLF (Financial), XLV (Healthcare), XLE (Energy), XLI (Industrial)
- **Международные**: EFA, VEA, EEM, VWO, FXI
- **Облигации**: AGG, BND, TLT, IEF, SHY
- **Драгоценные металлы**: GLD, SLV, GDX
- **Тематические**: ICLN (Clean Energy), LIT (Lithium), BOTZ (AI & Robotics)

### 6. **database.js** (главный файл)
Объединяет все базы данных и предоставляет API для работы с активами.

## API методы

### AssetDatabase класс

```javascript
const db = new AssetDatabase();

// Инициализация
await db.initialize();

// Проверка поддержки актива
await db.isAssetSupported('BTC');  // true/false

// Получить информацию об активе
db.getAssetInfo('AAPL');  // { symbol, name, type, sector, ... }

// Получить тип актива
await db.getAssetType('EURUSD');  // 'forex'

// Поиск активов (автодополнение)
db.searchAssets('BTC', 10);  // Возвращает массив результатов

// Получить все активы по типу
db.getAllCrypto();     // Все криптовалюты
db.getAllStocks();     // Все акции
db.getAllIndices();    // Все индексы
db.getAllForex();      // Все форекс пары
db.getAllETF();        // Все ETF

// Получить активы по типу (универсальный метод)
db.getAssetsByType('crypto');  // Все криптовалюты

// Получить популярные активы
db.getPopularAssets(20);  // Top 20 активов

// Общее количество активов
db.getTotalAssets();  // 400+
```

## Формат данных

### Криптовалюты
```javascript
{
    symbol: 'BTC',
    name: 'Bitcoin',
    pair: 'USDT'
}
```

### Акции
```javascript
{
    symbol: 'AAPL',
    name: 'Apple Inc.',
    sector: 'Technology'
}
```

### Индексы
```javascript
{
    symbol: '^GSPC',
    name: 'S&P 500',
    region: 'US',
    description: 'Standard & Poor\'s 500'
}
```

### Форекс
```javascript
{
    symbol: 'EURUSD',
    name: 'Euro / US Dollar',
    category: 'Major'
}
```

### ETF
```javascript
{
    symbol: 'SPY',
    name: 'SPDR S&P 500 ETF Trust',
    category: 'Broad Market',
    focus: 'Large Cap'
}
```

## Преимущества новой системы

✅ **Автоматизация**: Все активы в готовой базе данных, не требуется ручной ввод  
✅ **Скорость**: Мгновенная проверка поддержки активов без API запросов  
✅ **Масштабируемость**: Легко добавлять новые активы в соответствующие файлы  
✅ **Организация**: Активы разделены по категориям в отдельных файлах  
✅ **Умная проверка**: Многоуровневая система проверки активов  
✅ **Поиск**: Быстрый поиск по символу или названию актива  
✅ **400+ активов**: Покрывает все основные рынки

## Что происходит, если актив не в базе данных?

### Многоуровневая система проверки:

Если пользователь вводит актив, которого нет в нашей базе данных, система **автоматически** проверяет его через **5 внешних API** в следующем порядке:

#### 1️⃣ **Binance API** (криптовалюты)
- Проверяет пары: USDT, BUSD, BTC, ETH, FDUSD
- Пример: Если ввести `PEPE2` (новая монета), система проверит `PEPE2USDT`, `PEPE2BUSD` и т.д.
- ✅ Если найдено → актив поддерживается

#### 2️⃣ **CoinGecko API** (криптовалюты)
- Проверяет по ID и через поиск
- Находит даже редкие криптовалюты
- ✅ Если найдено → актив поддерживается

#### 3️⃣ **Yahoo Finance API** (акции, индексы, форекс)
- Проверяет акции, индексы и форекс пары
- Для форекс автоматически добавляет суффикс `=X`
- Пример: `JPYUSD` → проверит как `JPYUSD=X`
- ✅ Если найдено → актив поддерживается

#### 4️⃣ **Alpha Vantage API** (акции, форекс)
- Проверяет акции через Global Quote
- Проверяет форекс пары (6 символов)
- Пример: `USDKZT` → разбивает на `USD` и `KZT`
- ✅ Если найдено → актив поддерживается

#### 5️⃣ **Twelve Data API** (универсальный)
- Проверяет все типы активов
- Для форекс пробует формат с `/`
- Пример: `USDKZT` → проверит как `USD/KZT`
- ✅ Если найдено → актив поддерживается

### Примеры работы:

#### ✅ Пример 1: Новая криптовалюта (не в базе)
```
Пользователь вводит: MEME2
1. Проверка в базе данных → не найдено
2. Проверка Binance: MEME2USDT → найдено! ✅
Результат: Актив поддерживается, анализ запускается
```

#### ✅ Пример 2: Экзотическая форекс пара (не в базе)
```
Пользователь вводит: USDKZT
1. Проверка в базе данных → не найдено
2. Проверка Binance → не найдено
3. Проверка CoinGecko → не найдено
4. Проверка Yahoo Finance: USDKZT=X → найдено! ✅
Результат: Актив поддерживается, анализ запускается
```

#### ✅ Пример 3: Малоизвестная акция (не в базе)
```
Пользователь вводит: PLTR2
1. Проверка в базе данных → не найдено
2. Проверка через API → не найдено
Результат: ❌ Актив не поддерживается, показываем ошибку
```

### Логирование в консоли:

Система выводит подробные логи в консоль браузера:

```
🔍 Проверяем актив NEWCOIN через внешние API...
✅ Найдено на Binance: NEWCOINUSDT
```

или

```
🔍 Проверяем актив USDKZT через внешние API...
⚠️ Binance: не найдено
⚠️ CoinGecko: не найдено
✅ Найдено на Yahoo Finance (форекс): USDKZT=X
```

### Преимущества такого подхода:

✅ **Гибкость**: Поддерживает даже новые активы, которых нет в базе  
✅ **Надежность**: 5 разных API - если один не работает, проверит другие  
✅ **Универсальность**: Работает с криптовалютами, акциями, форекс, индексами  
✅ **Скорость**: Сначала проверяет базу данных (мгновенно), API только если нужно  
✅ **Прозрачность**: Все проверки логируются в консоль  
✅ **Автоматизация**: Пользователь ничего не замечает, все работает автоматически

## Как добавить новые активы

### Добавить криптовалюту
Откройте `crypto-database.js` и добавьте:
```javascript
{ symbol: 'NEW', name: 'New Coin', pair: 'USDT' }
```

### Добавить акцию
Откройте `stocks-database.js` и добавьте:
```javascript
{ symbol: 'NEW', name: 'New Company', sector: 'Technology' }
```

### Добавить форекс пару
Откройте `forex-database.js` и добавьте:
```javascript
{ symbol: 'NEWUSD', name: 'New Currency / US Dollar', category: 'Exotic' }
```

### Добавить индекс
Откройте `indices-database.js` и добавьте:
```javascript
{ symbol: '^NEW', name: 'New Index', region: 'Country', description: 'Description' }
```

### Добавить ETF
Откройте `etf-database.js` и добавьте:
```javascript
{ symbol: 'NEW', name: 'New ETF', category: 'Category', focus: 'Focus' }
```

## Совместимость

Класс `AssetDatabase` полностью совместим со старым `AssetService`:
```javascript
// Старый код продолжит работать
const assetService = new AssetService();
await assetService.isAssetSupported('BTC');
```

## Статистика базы данных

- 📈 **Криптовалюты**: 100+ монет
- 📊 **Акции**: 150+ компаний
- 📉 **Индексы**: 35+ индексов
- 💱 **Форекс**: 50+ валютных пар
- 🏦 **ETF**: 80+ фондов
- **ВСЕГО**: 400+ активов

---

**Создано**: Ноябрь 2025  
**Версия**: 2.0  
**Статус**: ✅ Готово к использованию
