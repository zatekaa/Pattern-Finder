# 📋 Пошаговая инструкция по развертыванию

## 1️⃣ Создайте .env файл

Скопируйте `.env.example` и переименуйте в `.env`:

```bash
copy .env.example .env
```

Откройте `.env` и вставьте ваши текущие API ключи:

```env
COINMARKETCAP_API_KEY=6bf5e09d04bb451e9b3fc16cb4e6a32c
ALPHA_VANTAGE_API_KEY=demo
TWELVE_DATA_API_KEY=0e5195250ca847a58fc5c79cd2c85d0e
POLYGON_API_KEY=q3doX5OW_Sp2bwk3iSXEBYLKzWPuWxIs
FINNHUB_API_KEY=d45j1ppr01qsugtajn70d45j1ppr01qsugtajn7g
```

## 2️⃣ Установите зависимости

```bash
npm install
```

## 3️⃣ Локальное тестирование

```bash
netlify dev
```

Откройте: http://localhost:8888

## 4️⃣ Развертывание на Netlify

### Вариант A: Через Netlify CLI

```bash
# Войдите в Netlify
netlify login

# Инициализируйте проект
netlify init

# Разверните
netlify deploy --prod
```

### Вариант B: Через Netlify Dashboard

1. Перейдите на [app.netlify.com](https://app.netlify.com)
2. Нажмите "Add new site" → "Deploy manually"
3. Перетащите папку проекта в окно браузера
4. Дождитесь завершения деплоя

## 5️⃣ Настройте Environment Variables

После развертывания:

1. Откройте ваш сайт в [Netlify Dashboard](https://app.netlify.com)
2. Перейдите: **Site settings** → **Environment variables**
3. Нажмите **Add a variable**
4. Добавьте каждый ключ:

| Key | Value |
|-----|-------|
| `COINMARKETCAP_API_KEY` | `6bf5e09d04bb451e9b3fc16cb4e6a32c` |
| `ALPHA_VANTAGE_API_KEY` | `demo` |
| `TWELVE_DATA_API_KEY` | `0e5195250ca847a58fc5c79cd2c85d0e` |
| `POLYGON_API_KEY` | `q3doX5OW_Sp2bwk3iSXEBYLKzWPuWxIs` |
| `FINNHUB_API_KEY` | `d45j1ppr01qsugtajn70d45j1ppr01qsugtajn7g` |

5. Нажмите **Save**
6. Перезапустите деплой: **Deploys** → **Trigger deploy** → **Deploy site**

## 6️⃣ Проверка работы

1. Откройте ваш сайт (URL будет вида: `https://your-site-name.netlify.app`)
2. Войдите (логин: `trader`, пароль: `Murod777`)
3. Попробуйте проанализировать актив (например: `BTC`)
4. Проверьте, что данные загружаются

## 🔍 Проверка Functions

В Netlify Dashboard:
1. Перейдите в **Functions**
2. Вы должны увидеть 7 функций:
   - binance
   - coinmarketcap
   - polygon
   - finnhub
   - twelvedata
   - alphavantage
   - yahoo
3. Кликните на любую функцию → **Logs** для просмотра логов

## ⚠️ Важные замечания

### Безопасность:
- ✅ Файл `.env` НЕ должен попадать в Git (он в `.gitignore`)
- ✅ API ключи хранятся только на сервере Netlify
- ✅ Frontend код больше не содержит ключей

### Лимиты API:
- **Binance**: Без ограничений для публичных данных
- **CoinMarketCap**: 333 запроса/день (бесплатный план)
- **TwelveData**: 800 запросов/день (бесплатный план)
- **Polygon**: 5 запросов/минуту (бесплатный план)
- **Finnhub**: 60 запросов/минуту (бесплатный план)

### Если что-то не работает:

1. **Проверьте Environment Variables**:
   - Все ли ключи добавлены?
   - Нет ли опечаток в названиях?

2. **Проверьте логи Functions**:
   - Dashboard → Functions → выберите функцию → Logs

3. **Проверьте консоль браузера**:
   - F12 → Console → ищите ошибки

4. **Перезапустите деплой**:
   - Deploys → Trigger deploy → Clear cache and deploy site

## 🎉 Готово!

Теперь ваше приложение полностью работает на Netlify с безопасным хранением API ключей!

### Ваш сайт доступен по адресу:
```
https://your-site-name.netlify.app
```

### Логин:
- **Username**: trader
- **Password**: Murod777
