# API Исправления для Vercel

## Проблема
Приложение было настроено для Netlify Functions (`/.netlify/functions/`), но развернуто на Vercel (`/api/`), что вызывало следующие ошибки:
- **Finnhub API**: 500 Internal Server Error с HTML вместо JSON
- **Yahoo Finance API**: 404 Not Found
- **FMP API**: 403 Forbidden (проблема с API ключом)
- **Alpha Vantage API**: Также не работал корректно

## Внесенные изменения

### 1. Frontend API Client (`frontend/src/utils/api-client.js` и `frontend/public/src/utils/api-client.js`)

**Изменения:**
- ✅ Добавлена автоматическая определение платформы (Vercel/Netlify)
- ✅ Изменен базовый URL на `/api` для Vercel
- ✅ Улучшена обработка ошибок для non-JSON ответов
- ✅ Добавлены логи для отладки

```javascript
// До:
this.baseURL = isLocal ? `...` : '/.netlify/functions';

// После:
const isVercel = window.location.hostname.includes('vercel.app');
if (isLocal) {
    this.baseURL = `http://localhost:${window.location.port || 9999}/api`;
} else if (isVercel) {
    this.baseURL = '/api';
} else {
    this.baseURL = '/.netlify/functions';
}
```

### 2. API Endpoints (Все файлы в `api/`)

**Обновленные файлы:**
- ✅ `api/finnhub.js`
- ✅ `api/yahoo.js`
- ✅ `api/alphavantage.js`
- ✅ `api/polygon.js`

**Изменения:**
- ✅ Добавлен заголовок `Content-Type: application/json` для всех ответов
- ✅ Проверка типа контента перед парсингом JSON
- ✅ Улучшенная обработка ошибок с детальными логами
- ✅ Всегда возвращается JSON, даже при ошибках

## Следующие шаги для развертывания

### 1. Проверка переменных окружения на Vercel

Убедитесь, что следующие переменные установлены в Vercel Dashboard:
```
FINNHUB_API_KEY=ваш_ключ
ALPHA_VANTAGE_API_KEY=ваш_ключ
POLYGON_API_KEY=ваш_ключ
APP_USERNAME=trader
APP_PASSWORD=Murod777
```

**Важно:** FMP API вернул 403, что означает:
- Либо API ключ недействителен
- Либо превышен лимит запросов
- Либо требуется платная подписка

### 2. Развертывание на Vercel

```bash
# Коммит изменений
git add .
git commit -m "Fix API endpoints for Vercel deployment"

# Push в репозиторий
git push

# Vercel автоматически развернет изменения
```

### 3. Тестирование

После развертывания проверьте в консоли браузера:
- ✅ `🔌 API Client initialized: /api` - должно показать правильный путь
- ✅ Запросы должны идти на `/api/finnhub`, `/api/yahoo`, и т.д.
- ✅ Ответы должны быть в формате JSON

### 4. Локальная разработка

Для локального тестирования с Vercel CLI:
```bash
# Установить Vercel CLI
npm install -g vercel

# Запустить локально
vercel dev
```

## Что было исправлено

### Проблема #1: Неправильный путь к API
- **Причина:** Frontend вызывал `/.netlify/functions/` вместо `/api/`
- **Решение:** Добавлена автоматическая определение платформы

### Проблема #2: HTML вместо JSON
- **Причина:** Ошибки API возвращали HTML-страницы Vercel
- **Решение:** Добавлен заголовок `Content-Type: application/json` и проверки

### Проблема #3: Отсутствие API ключей
- **Причина:** Переменные окружения не настроены на Vercel
- **Решение:** Нужно добавить их в Vercel Dashboard

### Проблема #4: Плохая обработка ошибок
- **Причина:** Frontend не мог корректно обработать non-JSON ответы
- **Решение:** Добавлена проверка типа контента и логирование

## Дополнительные рекомендации

1. **API ключи:** Проверьте, что все API ключи действительны:
   - Finnhub: https://finnhub.io/dashboard
   - Alpha Vantage: https://www.alphavantage.co/support/#api-key
   - Polygon: https://polygon.io/dashboard/api-keys
   - FMP: https://financialmodelingprep.com/developer/docs

2. **Лимиты API:** Проверьте лимиты запросов:
   - Finnhub Free: 60 запросов/минуту
   - Alpha Vantage Free: 5 запросов/минуту, 500 запросов/день
   - FMP Free: часто ограничен

3. **Fallback данные:** Приложение автоматически генерирует fallback данные при ошибках API

## Проверка работы

После развертывания откройте консоль браузера и проверьте:
```javascript
// Должно показать инициализацию
🔌 API Client initialized: /api

// При запросах должно показывать
🔌 Calling /api/finnhub { endpoint: '...', params: {...} }
```

Если все работает корректно, ошибки "<!DOCTYPE "... is not valid JSON" должны исчезнуть.
