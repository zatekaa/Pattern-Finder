# 🚀 Инструкция по деплою через GitHub

## 📋 СОДЕРЖАНИЕ

1. [Первоначальная настройка](#первоначальная-настройка)
2. [Загрузка кода на GitHub](#загрузка-кода-на-github)
3. [Подключение к Netlify](#подключение-к-netlify)
4. [Работа с кодом](#работа-с-кодом)
5. [Работа с нескольких компьютеров](#работа-с-нескольких-компьютеров)
6. [Решение проблем](#решение-проблем)

---

## 1️⃣ ПЕРВОНАЧАЛЬНАЯ НАСТРОЙКА

### ✅ Шаг 1.1: Проверьте Git

Git уже установлен! Проверьте версию:

```powershell
git --version
```

Должно показать: `git version 2.43.0.windows.1` или выше

---

### ✅ Шаг 1.2: Создайте аккаунт на GitHub

Если у вас нет аккаунта:

1. Откройте: **https://github.com/signup**
2. Введите email
3. Создайте пароль
4. Выберите username (например: `murod777`)
5. Подтвердите email

**Запомните ваш username!** Он понадобится дальше.

---

## 2️⃣ ЗАГРУЗКА КОДА НА GITHUB

### ✅ Шаг 2.1: Создайте репозиторий

1. Откройте: **https://github.com/new**

2. Заполните форму:
   ```
   Repository name: murodtrader-app
   Description: AI Pattern Finder for Financial Markets - 750+ Assets
   Public ✅ (чтобы показывать работодателям)
   
   ❌ НЕ СТАВЬТЕ галочки:
   - Add a README file
   - Add .gitignore
   - Choose a license
   ```

3. Нажмите: **"Create repository"**

4. **НЕ ЗАКРЫВАЙТЕ** эту страницу!

---

### ✅ Шаг 2.2: Подключите локальный репозиторий к GitHub

Откройте PowerShell в папке проекта:

```powershell
cd "c:\Users\HP\Desktop\second version"
```

Выполните команду (замените `YOUR_USERNAME` на ваш GitHub username):

```powershell
git remote add origin https://github.com/YOUR_USERNAME/murodtrader-app.git
```

**Пример:**
```powershell
git remote add origin https://github.com/murod777/murodtrader-app.git
```

---

### ✅ Шаг 2.3: Загрузите код на GitHub

```powershell
git push -u origin main
```

**Что произойдет:**
- GitHub может попросить ввести логин и пароль
- Или откроется окно авторизации
- Код загрузится на GitHub

**Проверьте:**
Откройте `https://github.com/YOUR_USERNAME/murodtrader-app` - там должен быть ваш код!

---

## 3️⃣ ПОДКЛЮЧЕНИЕ К NETLIFY

### ✅ Шаг 3.1: Откройте настройки Netlify

1. Перейдите: **https://app.netlify.com/sites/murod77/settings**
2. Нажмите: **"Build & deploy"** в левом меню
3. Найдите секцию: **"Continuous deployment"**

---

### ✅ Шаг 3.2: Подключите GitHub репозиторий

1. Нажмите: **"Link repository"** или **"Link site to Git"**
2. Выберите: **GitHub**
3. Авторизуйте Netlify (если попросит)
4. Выберите репозиторий: **murodtrader-app**
5. Настройки деплоя:
   ```
   Branch to deploy: main
   Build command: (оставьте пустым)
   Publish directory: public
   Functions directory: functions
   ```
6. Нажмите: **"Deploy site"**

---

### ✅ Шаг 3.3: Дождитесь первого деплоя

Netlify автоматически:
- Скачает код с GitHub
- Соберет проект
- Задеплоит на https://murod77.netlify.app

Это займет 1-2 минуты.

**Проверьте:**
Откройте https://murod77.netlify.app - сайт должен работать!

---

## 4️⃣ РАБОТА С КОДОМ

### ✅ Способ 1: Через скрипт (РЕКОМЕНДУЕТСЯ)

После любых изменений в коде:

```powershell
.\update.ps1
```

Скрипт:
1. Покажет измененные файлы
2. Попросит описать изменения
3. Создаст коммит
4. Загрузит на GitHub
5. Netlify автоматически задеплоит!

---

### ✅ Способ 2: Вручную

```powershell
# 1. Посмотреть что изменилось
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит с описанием
git commit -m "Описание изменений"

# 4. Загрузить на GitHub
git push

# 5. Netlify автоматически задеплоит через 1-2 минуты!
```

---

### 📝 Примеры коммитов:

```powershell
git commit -m "Добавил новые криптовалюты"
git commit -m "Исправил ошибку в анализаторе"
git commit -m "Обновил дизайн главной страницы"
git commit -m "Добавил поддержку новых индексов"
```

---

## 5️⃣ РАБОТА С НЕСКОЛЬКИХ КОМПЬЮТЕРОВ

### 🖥️ На новом компьютере (первый раз):

#### Шаг 1: Установите Git
```powershell
# Скачайте с https://git-scm.com/download/win
# Установите
```

#### Шаг 2: Настройте Git
```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

#### Шаг 3: Клонируйте репозиторий
```powershell
# Перейдите в нужную папку
cd C:\Projects

# Клонируйте репозиторий (замените YOUR_USERNAME)
git clone https://github.com/YOUR_USERNAME/murodtrader-app.git

# Перейдите в папку проекта
cd murodtrader-app
```

#### Шаг 4: Установите зависимости (опционально)
```powershell
# Если хотите запускать локально
npm install
cd functions
npm install
cd ..
```

**Готово!** Теперь можете работать с кодом.

---

### 🔄 Ежедневная работа с нескольких компьютеров:

#### На работе (утром):
```powershell
# 1. Получите последние изменения
git pull

# 2. Работайте с кодом
# ...

# 3. Загрузите изменения
.\update.ps1
# или
git add .
git commit -m "Работа на работе"
git push
```

#### Дома (вечером):
```powershell
# 1. Получите изменения с работы
git pull

# 2. Работайте с кодом
# ...

# 3. Загрузите изменения
.\update.ps1
# или
git add .
git commit -m "Работа дома"
git push
```

---

## 6️⃣ РЕШЕНИЕ ПРОБЛЕМ

### ❌ Ошибка: "git is not recognized"

**Решение:**
1. Перезапустите PowerShell
2. Или добавьте Git в PATH вручную:
   ```powershell
   $env:Path += ";C:\Program Files\Git\cmd"
   ```

---

### ❌ Ошибка: "remote origin already exists"

**Решение:**
```powershell
# Удалите старый remote
git remote remove origin

# Добавьте новый
git remote add origin https://github.com/YOUR_USERNAME/murodtrader-app.git
```

---

### ❌ Ошибка: "failed to push some refs"

**Причина:** На GitHub есть изменения, которых нет у вас локально.

**Решение:**
```powershell
# Получите изменения с GitHub
git pull --rebase

# Загрузите свои изменения
git push
```

---

### ❌ Ошибка: "merge conflict"

**Причина:** Вы и кто-то другой изменили один и тот же файл.

**Решение:**
1. Откройте файл с конфликтом
2. Найдите строки с `<<<<<<<`, `=======`, `>>>>>>>`
3. Выберите нужную версию
4. Удалите маркеры конфликта
5. Сохраните файл
6. Выполните:
   ```powershell
   git add .
   git commit -m "Resolved conflict"
   git push
   ```

---

### ❌ Netlify не деплоит изменения

**Проверьте:**

1. **Код загружен на GitHub?**
   ```powershell
   git push
   ```

2. **Netlify подключен к GitHub?**
   - Откройте: https://app.netlify.com/sites/murod77/settings
   - Проверьте секцию "Build & deploy"

3. **Посмотрите логи деплоя:**
   - https://app.netlify.com/sites/murod77/deploys
   - Нажмите на последний деплой
   - Проверьте ошибки

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

### Просмотр истории:
```powershell
git log --oneline
```

### Откат последнего коммита:
```powershell
git reset --soft HEAD~1
```

### Посмотреть изменения:
```powershell
git diff
```

### Посмотреть статус:
```powershell
git status
```

### Посмотреть remote URL:
```powershell
git remote -v
```

---

## 🎯 КРАТКАЯ ШПАРГАЛКА

### Первый раз:
```powershell
1. Создать репозиторий на GitHub
2. git remote add origin https://github.com/USERNAME/murodtrader-app.git
3. git push -u origin main
4. Подключить к Netlify
```

### Каждый день:
```powershell
1. git pull          # Получить изменения
2. # Работа с кодом
3. .\update.ps1      # Загрузить изменения
```

### На новом компьютере:
```powershell
1. Установить Git
2. git clone https://github.com/USERNAME/murodtrader-app.git
3. cd murodtrader-app
4. Работать!
```

---

## ✅ ПРОВЕРКА НАСТРОЙКИ

После настройки проверьте:

- [ ] Git установлен: `git --version`
- [ ] Репозиторий создан на GitHub
- [ ] Код загружен: `https://github.com/USERNAME/murodtrader-app`
- [ ] Netlify подключен к GitHub
- [ ] Сайт работает: `https://murod77.netlify.app`
- [ ] Автодеплой работает (измените файл → push → через 2 мин на сайте)

---

## 🎉 ГОТОВО!

Теперь у вас:
- ✅ Код на GitHub (безопасное хранение)
- ✅ Автоматический деплой через Netlify
- ✅ Возможность работать с любого компьютера
- ✅ История всех изменений
- ✅ Профессиональный workflow

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не работает:
1. Проверьте раздел "Решение проблем"
2. Посмотрите логи: `git log`, `git status`
3. Проверьте Netlify логи: https://app.netlify.com/sites/murod77/deploys

---

**Автор:** MurodTrader  
**Дата:** 2025  
**Версия:** 1.0
